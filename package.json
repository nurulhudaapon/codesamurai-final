{
  "name": "ecosync",
  "description": "A monorepo for the Ecosync - an app to to bridge the gaps, streamline processes, and enhance accountability through technological innovation.",
  "version": "0.0.0-alpha-0",
  "module": "index.ts",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start:container": "docker compose up -d",
    "start:web": "cd packages/web && bun run dev",
    "reset": "docker compose up -d && cd packages/db && prisma migrate reset --force",
    "dev": "bun run start:container && bun run start:web"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@types/uuid": "^9.0.8",
    "bun-types": "latest",
    "prettier": "^3.2.5",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "clsx": "^2.1.0",
    "kleur": "^4.1.5",
    "next-auth": "^4.24.7",
    "react-hot-toast": "^2.4.1",
    "tailwind-merge": "^2.2.2",
    "uuid": "^9.0.1"
  }
}
