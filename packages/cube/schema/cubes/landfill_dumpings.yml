cubes:
  - name: landfill_dumpings
    sql_table: public.landfill_dumpings
    data_source: default

    joins: []

    dimensions:
      - name: id
        sql: id
        type: string
        primary_key: true

      - name: created_by_user_id
        sql: created_by_user_id
        type: string

      - name: created_at
        sql: created_at
        type: time

      - name: updated_at
        sql: updated_at
        type: time

      - name: arrival_time
        sql: arrival_time
        type: time

      - name: departure_time
        sql: departure_time
        type: time

    measures:
      - name: count
        type: count

    pre_aggregations:
      # Pre-aggregation definitions go here.
      # Learn more in the documentation: https://cube.dev/docs/caching/pre-aggregations/getting-started

